using UnityEngine;

namespace CodeBase.Logic
{
    public class InitializePoint : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
